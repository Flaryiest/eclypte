"""Storage substrate for the Eclypte control plane.

Import from the submodules directly (api.storage.repository, api.storage.refs,
api.storage.factory, ...) — every consumer in the repo does, so this package
deliberately re-exports nothing.
"""
