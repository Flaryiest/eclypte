# Local dev deps (Windows native, macOS, Linux — all fine).
# The heavy ML stack (torch, allin1, madmom, natten, librosa, etc.) lives in
# requirements-modal.txt and runs inside a Modal container — not here.

modal
pytubefix
pydub
syncedlyrics
opencv-python
scenedetect
openai
python-dotenv
pydantic>=2
pyyaml
moviepy>=2